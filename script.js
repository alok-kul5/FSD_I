// smooth scrolventListener('click', function (e) {
    e.preventl
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEDefault();
    dont.cumequerySelector(this.getAttribute('href')).scrollIntoView({
      behavior: 'smooth'
    });
  });
});
